#pragma once
class course
{
public:
	course();
	~course();
	void course::Shuffle(std::vector<int> &vec);
	void course::Sendcard(vector<int> &vec1, vector<int> &vec2, vector<int> &vec3);
};

